version: "3.8"

services:
  humancheck-demo:
    build:
      context: .
      target: demo
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev --workspace=packages/demo
    environment:
      - NODE_ENV=development
